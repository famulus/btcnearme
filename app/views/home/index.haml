%h1
  BTC NEAR ME
  
%h2
  Find Bitcoin traders near you... settle in cash.
	

%hr
=form_for(:post, @post, :url => {:controller => :home,:action => :create_post}) do |f|
  =@post.errors.full_messages.join(", ")

  I want to 
  =f.select(:buying_or_selling, options_for_select(["buy","sell"]))
  /=f.text_field(:quantity)
  BTC
  %br/
  Near zip code: 
  =f.text_field(:zip_code)

  %br/
  Interested traders can contact me at this email:
  =f.text_field(:email,{:subject => "Trade BTC near #{cookies[:zip_code]}"})
  =submit_tag("Post")
  %br/
  (email listed publicly)

%hr



-if @posts.present?
  %p
    Traders near 
    =cookies[:zip_code]

  #left
    %h3 Buying BTC
    %table
      %th Distance
      %th Email
      -@posts.select{|p| p.buying_or_selling == "buy"}.each do |post|
        %tr
          %td="#{number_with_precision(post.distance, {:precision =>1 })} kilometers"
          %td=mail_to(post.email)
      
  #right
    %h3 Selling BTC
  
    %table
      %th Distance
      %th Email
      -@posts.select{|p| p.buying_or_selling == "sell"}.each do |post|
        %tr
          %td="#{number_with_precision(post.distance, {:precision =>1 })} kilometers"
          %td=mail_to(post.email)
  .clear
    
/ -if @posts.blank?
/   You are the first person to trade BTC in this area! 
  